type User {
  _id: ID!
  username: String!
  email: String
  avatar: String
  description: String
  followingsCount: Int
  followersCount: Int
  collectionsCount: Int
  createdAt: Date
  updatedAt: Date
}

type Query {
  user(id: ID!): User
}

